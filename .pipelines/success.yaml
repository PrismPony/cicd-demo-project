pipeline:
  name: demo-success-pipeline

stages:
  - test

# Jobs must be defined at the root level as a list of properties
run-tests:
  - stage: test
  - image: node:18-alpine
  - script:
      - echo "All tests passed"
