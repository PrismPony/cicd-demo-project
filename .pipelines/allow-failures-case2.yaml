pipeline:
  name: allow-failures-case2
stages:
  - build
job-a:
  - stage: build
  - image: alpine
  - script: exit 1
  - failures: true
job-b:
  - stage: build
  - image: alpine
  - script: echo "This runs successfully despite job-a failing."
  - needs: [job-a]
  - failures: false
